// Placeholder for main page JS, e.g., form validation or enhancements
document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('join-form');
    form.addEventListener('submit', (e) => {
        // Basic validation or enhancements can be added here
        // Currently, HTML5 validation handles required fields
    });
});